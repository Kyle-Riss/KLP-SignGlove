# 체크포인트 및 Scaler 파일 분석 리포트

## 📊 확인된 문제점

### 1. 체크포인트 불일치: GRU와 StackedGRU가 같은 체크포인트 사용

**문제:**
- `archive/checkpoints_backup/checkpoints_backup/GRU_best.ckpt`는 실제로 **StackedGRU**입니다 (2개 레이어: l0, l1)
- 실제 **GRU** 체크포인트 (단일 레이어)가 없습니다

**확인 결과:**
```
archive/checkpoints_backup/checkpoints_backup/GRU_best.ckpt:
  모델 타입: StackedGRU
  RNN l0 레이어: 4
  RNN l1 레이어: 4
  Epoch: 65
  총 파라미터 수: 74,776
```

**해결 방안:**
1. 실제 GRU 체크포인트를 찾거나 새로 학습
2. 또는 혼동 행렬 생성 시 GRU 대신 StackedGRU 사용
3. 혼동 행렬 스크립트에서 GRU 체크포인트 경로를 StackedGRU로 수정

---

### 2. Scaler 파일 누락: 정규화가 적용되지 않음

**문제:**
- Scaler 파일 (`scaler.pkl`)이 전혀 없습니다
- 추론 시 정규화가 적용되지 않아 성능이 저하될 수 있습니다

**현재 상태:**
- `preprocess_data()` 함수에서 StandardScaler를 생성하지만 저장하지 않음
- 추론 엔진에서 scaler 파일을 찾지 못해 경고 발생

**해결 방안:**
1. 학습 시 scaler.pkl 파일을 저장하도록 수정
2. 또는 DynamicDataModule에서 scaler를 저장하도록 수정
3. 또는 테스트 데이터셋과 동일한 scaler를 사용하도록 수정

---

### 3. MS3DStackedGRU 모델 구조 불일치

**문제:**
- MS3DStackedGRU는 MS3DGRU 체크포인트를 사용 중
- 체크포인트 로드 시 파라미터 불일치 경고 발생:
  ```
  size mismatch for tower2.0.weight: copying a param with shape torch.Size([32, 1, 5, 5, 5]) 
  from checkpoint, the shape in current model is torch.Size([32, 1, 3, 5, 3]).
  ```

**확인된 체크포인트:**
- `best_model/ms3dgru_best.ckpt` - MS3DGRU (정상 작동, 99.13% 정확도)
- MS3DStackedGRU 전용 체크포인트 없음

**해결 방안:**
1. MS3DStackedGRU 전용 체크포인트 찾기
2. 또는 MS3DStackedGRU를 혼동 행렬 생성에서 제외
3. 또는 모델 구조를 체크포인트와 일치하도록 수정

---

## 📋 현재 사용 가능한 체크포인트

| 체크포인트 경로 | 실제 모델 타입 | 상태 | 파라미터 수 |
|---------------|--------------|------|------------|
| `archive/checkpoints_backup/checkpoints_backup/GRU_best.ckpt` | StackedGRU | ✅ 사용 가능 | 74,776 |
| `best_model/ms3dgru_best.ckpt` | MS3DGRU | ✅ 사용 가능 | 59,228 |
| `archive/checkpoints_backup/checkpoints_backup/MSCSGRU_best.ckpt` | MS3DGRU | ✅ 사용 가능 | 72,188 |

---

## 🔧 권장 해결 방안

### 즉시 수정 가능한 사항:

1. **혼동 행렬 스크립트 수정:**
   - GRU 체크포인트 경로를 StackedGRU로 인식하도록 수정
   - 또는 GRU 체크포인트를 찾을 수 없다는 경고 추가

2. **Scaler 파일 생성 스크립트 작성:**
   - DynamicDataModule에서 scaler를 저장하도록 수정
   - 또는 별도의 스크립트로 scaler.pkl 파일 생성

3. **MS3DStackedGRU 처리:**
   - 체크포인트가 없으므로 혼동 행렬 생성에서 제외
   - 또는 MS3DGRU 체크포인트를 사용하되 구조 불일치 경고 무시

### 장기 해결 방안:

1. **체크포인트 관리 개선:**
   - 모델별로 명확한 체크포인트 파일명 사용
   - 체크포인트에 모델 타입 메타데이터 저장

2. **Scaler 파일 자동 저장:**
   - 학습 시 scaler.pkl 파일 자동 저장
   - 체크포인트와 함께 저장

3. **모델 구조 일관성:**
   - 체크포인트와 모델 구조 일치 확인
   - 모델 구조 버전 관리






